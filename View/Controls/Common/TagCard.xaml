<UserControl x:Class="ToDo_Manager.View.Controls.Common.TagCard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="Auto" Width="Auto">

    <UserControl.Resources>

        <!-- Анимация появления карточки -->
        <Storyboard x:Key="CardAppearAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="10" To="0" Duration="0:0:0.3"/>
        </Storyboard>

        <!-- Тень карточки -->
        <DropShadowEffect x:Key="CardShadow"
                          Color="#000000"
                          BlurRadius="18"
                          ShadowDepth="2"
                          Opacity="0.20"/>
    </UserControl.Resources>

    <Border CornerRadius="12"
            Padding="12"
            Margin="6"
            Background="{Binding ColorHex}"
            Effect="{StaticResource CardShadow}"
            RenderTransformOrigin="0.5,0.5">

        <Border.RenderTransform>
            <TranslateTransform Y="10"/>
        </Border.RenderTransform>

        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource CardAppearAnimation}"/>
            </EventTrigger>
        </Border.Triggers>

        <StackPanel Orientation="Horizontal">

            <!-- Название тега -->
            <TextBlock Text="{Binding Name}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="White"
                       Margin="0 0 10 0"/>


            <Button 
        Command="{Binding DataContext.EditTagCommand,
                  RelativeSource={RelativeSource AncestorType=Page}}"
        CommandParameter="{Binding}"
        Background="Transparent"
        Padding="6 3"
        BorderThickness="0"
        Cursor="Hand"
        HorizontalAlignment="Right"
        Margin="0 0 6 0">

                <Viewbox Width="22" Height="22">
                    <Path Data="M3,17 L3,21 L7,21 L17,11 L13,7 z 
                    M14,6 L18,10 L20,8 L16,4 z"
              Fill="#FFEE58"/>
                </Viewbox>
            </Button>


            <!-- Кнопка удаления -->
            <Button 
                    Command="{Binding DataContext.DeleteTagCommand,
                              RelativeSource={RelativeSource AncestorType=Page}}"
                    CommandParameter="{Binding}"
                    Background="Transparent"                   
                    Padding="6 3"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Right">
                <Viewbox Width="22" Height="22">
                    <Path Data="M3,6 L21,6 L19,22 L5,22 z M8,6 L8,4 L16,4 L16,6" Fill="#C62828"/>
                </Viewbox>
            </Button>
        </StackPanel>

    </Border>
</UserControl>
